# The names of each country is taken from the official Olympics website. It does not imply any political view of the author of this code.
olympicMedalStatisticsByCountry <- data.frame(COUNTRY = c("", "", "", ""), MEDALCOUNT = c(), POPULATION = c(), GDP = c()) # We create a data frame to set this up for our Poisson regression modelling later.
